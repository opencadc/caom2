## deployable containers have a semantic and build tag
# semantic version tag: major.minor
# build version tag: timestamp
VER=2.0-BETA-04
TAGS="${VER} ${VER}-$(date -u +"%Y%m%dT%H%M%S")"
unset VER

# intent of VER tag: 
## will use ALPHA while implementation is incomplete
## will use BETA when implementation is ~complete and CAOM model is still WD
## will drop qualifier and use normal semver (2.0.0) when CAOM is a PR
